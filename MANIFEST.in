# The tests ship in the sdist, as they do for the driver and the connector, so a
# downstream packager can run them. conftest.py carries every fixture: without it the
# shipped tests collect and then error.
include LICENSE
include README.md
include MANIFEST.in
recursive-include tests *.py
